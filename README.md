# karavantrack-app

Web cabinet for YoolLive (shipper/carrier dashboard). React + TypeScript + Vite.

## Commands

- `npm run dev` — start the dev server
- `npm run build` — type-check (`tsc -b`) and build
- `npm run lint` — eslint
- `npm run preview` — preview a production build locally
