Replace TEAMID with the real Apple Developer Team ID before shipping Universal Links.

Also replace `REPLACE_WITH_REAL_RELEASE_SIGNING_SHA256_FINGERPRINT` in `assetlinks.json` with the
real Android release-signing SHA-256 certificate fingerprint before shipping Android App Links.
