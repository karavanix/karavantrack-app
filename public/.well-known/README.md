Universal Links (iOS) / App Links (Android) verification files for the
karavantrack-mobile driver app.

- `apple-app-site-association` — `appID` is `C262CX9W5K.live.yool.app`
  (Apple Team ID `C262CX9W5K`, bundle ID `live.yool.app`).
- `assetlinks.json` — `package_name` is `yool.live.app`, verified against
  the release-signing SHA-256 certificate fingerprint.

If the app is re-signed with a different certificate (e.g. a new release
keystore) or moves to a new bundle/package ID, update the values here to
match, otherwise Universal Links / App Links verification will silently
fail and invite/tracking links will fall back to opening in the browser.
